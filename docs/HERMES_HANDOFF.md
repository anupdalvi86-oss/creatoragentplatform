# Hermes handoff: current context and working agreement

Snapshot: 2026-09-20. Verify the checkout and live services again before acting; this file is not a credentials store or a substitute for inspecting code.

## Where everything lives

- GitHub repository: `anupdalvi86-oss/creatoragentplatform`; local checkout in this conversation: `/Users/anup.dalvi/CreatorAgentPlatform`. The Hermes Telegram session reported cloning it into `/data/CreatorAgentPlatform` on Railway. Confirm that path and its branch in the current Hermes session before using it; a container filesystem may not be durable.
- Production Cloudflare Worker/PWA: <https://creator-agent-platform.anupdalviind.workers.dev>. A public HTTP response was verified on 2026-09-20; that does not validate admin Access, secrets, D1 contents, model calls, or individual workflows.
- Hermes dashboard: <https://hermes-agent-production-f50d.up.railway.app/sessions>; Telegram bot is an engineering interface. The creator-facing PWA is **not** hosted by Hermes. The last observed app deployment was based on `9155d8d`; later documentation commits do not themselves deploy the app. Check `git log` and `git status` rather than assuming either revision is current.
- A parallel Codex task previously pushed and deployed that commit. The Hermes screenshot reported a clean clone/install/typecheck/test/lint/build with **14 tests** but expressly no push or deploy. Those checks are historical, not validation of future changes.
- Hermes had intermittent provider errors/rate limits. Kimi was used as primary in the observed setup. Merely adding OpenRouter does not create failover; configure and verify a fallback in Hermes independently. Do not expose credentials in Telegram or logs. The product Worker currently has an OpenAI-compatible adapter and mock fallback; Hermes provider configuration does not configure the product's AI calls.

## Code map and present behavior

- `src/client/App.tsx`, `AdminApp.tsx`: cooking PWA and operator interface. `src/server/worker.ts`: consumer/admin routes, gates, sessions and tenant resolution.
- `src/server/agent.ts`: fixed cooking prompt/tool registry and bounded `canMake`. `src/server/ai.ts`: OpenAI Chat Completions-compatible adapter, model policy, cost limit, timeout and deterministic mock fallback. `src/server/retrieval.ts`: tenant/rights-scoped retrieval, optional Vectorize.
- `src/server/domain.ts`: deterministic cooking operations. `src/server/research.ts`: Creator Scout with manual/official YouTube facts, optional external bridge and reviewable ProductSpec. `src/server/youtube.ts`: video **metadata** ingestion, not media/transcript downloading; its `TranscriptProvider` is an unused future interface.
- `migrations/`: D1 schema; `seed/demo.sql`: illustrative demo data, **not** real creator-authored recipes. `tests/`, `evals/`, `scripts/smoke.ts`: current checks. `wrangler.jsonc`: Worker, D1 and model vars; secrets are external. `npm run build` is a dry-run bundle; only `npm run deploy` publishes.
- Existing `agents` and `agent_tools` rows do not imply a generic multi-agent dispatcher. Admin edits the creator cooking agent; runtime tool names are fixed in `worker.ts` and `agent.ts`. No agent task queue, durable orchestration, transcript store, Instagram integration, browser E2E suite, or autonomous code-changing service is in this repository yet.

## Scope and boundaries

The owner's selected roles and explicit exclusions are in `AGENT_CATALOG.md`. Separate two execution planes: product agents run behind the Worker with creator-scoped permissions; engineering agents work on Git branches through Hermes and must never receive general production powers. Make the first implementation an end-to-end, small vertical slice, not 32 placeholder agents. Then build independent roles concurrently on separate branches/worktrees if supported, integrating against the shared contract. Use idempotent tasks, an explicit state machine, retries and budgets. Human review gates **new content rights and public publication**, payments/commercial actions, remote migrations and production release; routine authorized customer answers and internal read-only reports can run without individual approval.

Use authorized creator uploads/exported captions first for transcripts. Public YouTube metadata is not permission to download captions or media. Instagram needs an approved account connection and a validated access path; do not scrape around platform restrictions. Record language, source URL, time offsets, rights basis, provenance, review state, and failure reason. Keep draft text out of consumer retrieval until approved. Add an async mechanism (e.g. Cloudflare Workflows for multi-step jobs and Queues for fan-out) only after confirming account capabilities and cost. A separate transcription service may be needed for audio; decide after measuring workloads rather than assuming the Worker can process arbitrary video files.

Keep tenant predicates and origin/session/admin checks. Preserve deterministic cooking/grocery math and source-linked answers. Respect current safety rules: no fabricated creator statements, no allergy/medical assurances, no inferred audience numbers presented as facts, no invented payment or rights status. Minimize personal data in traces; report cost/latency and rate-limit behavior without logging prompts or keys.

## How to work through the backlog

1. **Foundation dependency**: reconcile docs with code, design narrow role/task/run contracts, implement one authorized admin-triggered read-only Data Quality report or reversible Content Librarian task end to end, with a creator-scoped API, idempotency, stored status, limits and tests. The first result may be immediately visible to the operator; "draft-only" is not a blanket rule.
2. **Parallel tracks once the shared contracts are stable**: content (authorized media/captions, transcripts, extraction, translation, rights/source checks, librarian, connectors); consumer (reuse cooking, plans, grocery, then new learning/support roles); engineering (Hermes UI/UX, feature planning, frontend/backend, workflow tests); operations (routing, cost and data quality). Separate branches/worktrees and shared-interface ownership prevent competing migrations and edits. If parallel execution isn't supported, alternate small independent slices without claiming they ran concurrently.
3. **Incremental integration**: integrate each tested role as ready, not after an entire track is complete. Ordinary tenant-scoped responses may run live. Only newly ingested or derived creator content requires review before public retrieval. External permissions or credentials block that connector, not all other roles.

For each milestone: inspect the actual code, implement a working acceptance-tested slice, run `npm run typecheck && npm test && npm run lint && npm run build`, and report the diff, migrations, risks and next slice. Continue through safe in-scope work without waiting for approval on every routine code edit or agent response. Ask the owner before pushing, running a **remote** D1 migration, publishing content, authorizing rights, changing payments or deploying. Keep a concise `docs/AGENT_PROGRESS.md` log on your working branch with completed roles, remaining work, evidence and blockers. The untracked local `test.tmp` observed in the Codex checkout belongs to the user; leave it alone if present in your checkout.

## Full Telegram/console instruction

Paste this after the updated docs are pushed. The prompt is self-contained, but the repository files are the detailed source of truth.

```text
Work in /data/CreatorAgentPlatform. First inspect pwd, git status/branch/log. If the checkout has user changes, preserve them and do not overwrite anything. If clean, fast-forward pull origin/main; if a pull would conflict or authentication fails, stop and report the exact issue. Confirm AGENTS.md, docs/HERMES_HANDOFF.md and docs/AGENT_CATALOG.md are present and current. Read them and the linked README, architecture, runtime, data-model, Scout, implementation and deployment docs; compare documentation with the actual code/migrations/tests. Never print secrets.

Objective: implement the selected specialist-agent catalog as working, observable capabilities, excluding every role listed in AGENT_CATALOG.md's Explicit exclusions. Do not create 32 empty records or pretend existing partial features are new agents. Distinguish the Cloudflare product runtime from Hermes engineering workflows. Keep existing cooking, planning, grocery, admin and tenant APIs working.

Build the shared foundation first: allowlisted role/tool contracts; creator-scoped admin task submission and status/result; durable task/run state and audit trail; idempotency; bounded concurrency/retries, time/tokens/cost limits, and explicit failures. Use an additive D1 migration. Prove it end to end with one useful read-only Data Quality report or reversible Content Librarian task, visible to the authorized operator immediately. Test permissions, cross-tenant access, duplicate delivery, failure and budget caps. Briefly state the design/acceptance criteria, then implement without waiting for approval for ordinary code edits.

Once that contract is stable, work on independent tracks in parallel if you have isolated worktrees/sessions, or in alternating small slices otherwise: (A) authorized content intake, transcription, translation, recipe extraction, librarian, rights/source checks, repurposing, SEO, YouTube and Instagram connectors; (B) registering and extending the existing cooking, substitution, meal, grocery, pantry, voice and personalization functions, then learning/support; (C) reusable Hermes workflows for product strategy, feature specs, UI/UX, frontend/backend work, browser workflow tests, evaluations and release preparation; (D) orchestration, model routing, cost and data-quality monitoring. Coordinate any shared APIs/migrations, integrate tested slices as ready, and record progress in docs/AGENT_PROGRESS.md. Do not block unrelated tracks for missing platform credentials. Do not claim a role is working without a trigger, observable output and tests.

Do not apply a blanket draft-only restriction. Existing and new bounded customer answers and internal reports can run normally once authorized and tested. New transcripts, extracted creator content and derivatives stay in review until creator authorization and human approval; no public retrieval or publishing before that. Never invent creator claims, rights, audience metrics or payment status. Do not scrape around YouTube/Instagram restrictions. When credentials, account permissions or owner choices are genuinely missing, report precisely what is needed and continue other safe work.

For each implemented slice: use a feature branch, additive migration if needed, tests for success/failure/tenant isolation, and run npm run typecheck, npm test, npm run lint, npm run build. Build is a dry run. Summarize the change, tests, remaining limitations and next slice; continue safe work across milestones rather than stopping after the first role. Preserve unrelated files. Do not push/merge, run remote migrations, change secrets, publish content, change payment/commercial state or deploy to production without my explicit approval. Human approval is for those high-impact effects, not each routine response or code edit.
```
